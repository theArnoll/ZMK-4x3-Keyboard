#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

&kscan0 {
    debounce-press-ms = <1>;
    debounce-release-ms = <1>;
};

/ {
    keymap {
        compatible = "zmk,keymap";

        default_layer { // Layer 0
            // -------------------------------------
            // |     Z     |     M     |     K     |
            // |     A     |     B     |     C     |
            // |     D     |     E     |     F     |
            bindings = <
                &kp I    &kp 2    &kp 3
                &kp U    &kp 5    &kp J
                &kp T    &kp 8    &kp G
                &kp R    &kp 0    &studio_unlock
            >;
        };
    };
 };